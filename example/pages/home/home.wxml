<view wx:if="{{from}}">从{{from}}进入本页面</view>
<view wx:if="{{id}}">注册成功获得id:{{id}}</view>

<button
  type="primary"
  bindtap="loginOnTap"
>跳转至login</button>

<button
  type="primary"
  bindtap="cartOnTap"
>跳转至Cart</button>

<button
  type="primary"
  bindtap="detailsOnTap"
>跳转至Details</button>